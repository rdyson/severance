# Severance — config.yaml
# Copy this to config.yaml and fill in your keys.

providers:
  anthropic:
    # Admin API key (sk-ant-admin-...) from console.anthropic.com → Settings → Admin Keys
    admin_api_key: ""
  openai:
    # Admin API key from platform.openai.com → Settings → Organization → Admin Keys
    admin_api_key: ""
  # google:
  #   project_id: "my-project"
  #   credentials_path: "/path/to/service-account.json"

auth:
  username: "admin"
  password: "changeme"

server:
  host: "127.0.0.1"
  port: 8077

refresh:
  interval_hours: 6
  default_granularity: "1d"
